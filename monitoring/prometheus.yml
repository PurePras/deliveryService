global:
  scrape_interval: 15s

scrape_configs:
  - job_name: backend
    static_configs:
      # Compose service name — resolved via the internal Docker network, never exposed
      # to the host or through nginx.
      - targets: ["backend:8080"]
